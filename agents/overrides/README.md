# Project overrides

Project-local overrides/extensions of shared skills, rules, and commands. The only project-side agent surface ADR-020 permits besides the bridge marker. See the `override-management` skill.
